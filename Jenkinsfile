pipeline {
  agent any
  stages {
    stages("build") {
      steps {
        echo 'building the app ...'
      }
    }
    
    stages("test") {
      steps {
        echo 'testing the app ...'
      }
    }
    
    stages("deploy") {
      steps {
        echo 'deploying the app ...'
      }
    }
    
  }
}
